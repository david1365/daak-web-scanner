;(function( daak, window, document, undefined ) {
    daak.elems.zoomTool = {
        render: '~zoom-tool',

        body: function () {
            this.abc = function (value) {
                alert(value);
            }
        }
    }
}) (daak, window, document);
